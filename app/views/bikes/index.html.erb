<div class="container">
  <% @bikes.each do |bike| %>

      <div class="col-md-4">
        <div class="thumbnail">
          <img src="..." alt="...">
          <div class="caption">
            <h3><%= bike.build_name %></h3>
            <p><%= bike.description %></p>
            <p> <%= link_to "View Build", bike_path(bike.id) %> </p>
          </div>
        </div>
      </div>

  <% end %>
</div>
